const express = require('express');
const providersCtrl = require('../controllers/providers.controller');

const router = express.Router(); // eslint-disable-line new-cap

// router.route('/')
  // .get(providersCtrl.getProviders)

module.exports = router;

const express = require('express');
const plansPayorsCtrl = require('../controllers/plansPayors.controller');

const router = express.Router(); // eslint-disable-line new-cap

// router.route('/')
//   .get(plansPayorsCtrl.getPlansPayors)

module.exports = router;
